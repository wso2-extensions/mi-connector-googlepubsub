{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Cloud Pub/Sub API Create Topic Operation Response Schema",
  "description": "Schema representing the output of the Create Topic operation in Cloud Pub/Sub API",
  "properties": {
    "payload": {
      "description": "A topic resource.",
      "properties": {
        "name": {
          "description": "The name of the topic. It must have the format `\"projects/{project}/topics/{topic}\"`."
        }
      }
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}